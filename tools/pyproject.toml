[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "testbench"
version = "0.1.0"
description = "Embedded protocol test bench"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["testbench"]